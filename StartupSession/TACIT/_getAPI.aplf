 {res}←{larg}_getAPI(fn rarg)
 ⍝ load API-Function "fn" and execute with rarg and optional larg
 ⍝ any subroutines (code to be brought in from other files) will need to use :Require
 ⎕←'getAPI'


 :If 0=⎕NC'larg' ⋄ res←⍎'API.',fn,' rarg'
 :Else ⋄ res←⍎'larg API.',fn,' rarg'
 :EndIf
